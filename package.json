{
  "name": "luxury-watch-store",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "php -S localhost:8000",
    "build": "echo 'Build complete'",
    "preview": "php -S localhost:3000"
  }
}